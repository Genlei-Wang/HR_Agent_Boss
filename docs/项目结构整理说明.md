# 项目结构整理说明

> 📁 项目文件结构优化，便于后续功能扩展

---

## 🎯 整理目标

1. **支持多AI模型**：抽象AI服务接口，便于添加新模型
2. **模块化代码**：按功能划分，便于维护和扩展
3. **功能扩展**：为沟通列表等功能预留接口
4. **代码复用**：提取公共逻辑，减少重复代码

---

## 📁 新的目录结构

```
boss-greeting-assistant/
├── src/
│   ├── background/              # 后台服务
│   │   ├── services/           # AI服务实现（新增）
│   │   │   ├── base-ai-service.ts      # AI服务基类
│   │   │   ├── gemini-service.ts       # Gemini实现
│   │   │   ├── qwen-service.ts         # 通义千问（待实现）
│   │   │   ├── kimi-service.ts         # Kimi（待实现）
│   │   │   ├── deepseek-service.ts     # DeepSeek（待实现）
│   │   │   ├── wenxin-service.ts       # 文心一言（待实现）
│   │   │   └── zhipu-service.ts        # 智谱GLM（待实现）
│   │   ├── index.ts            # 主入口（已重构）
│   │   ├── screenshot-service.ts
│   │   └── ...
│   │
│   ├── content/                # 内容脚本
│   │   ├── features/           # 功能模块（新增）
│   │   │   ├── chat-list-handler.ts    # 沟通列表处理（待实现）
│   │   │   └── ...
│   │   ├── automation-controller.ts
│   │   ├── candidate-handler.ts
│   │   └── ...
│   │
│   ├── shared/                 # 共享代码
│   │   ├── ai-service.interface.ts     # AI服务接口（新增）
│   │   ├── ai-config.ts                # AI模型配置（新增）
│   │   ├── types.ts
│   │   ├── constants.ts
│   │   └── ...
│   │
│   └── sidepanel/              # 侧边栏UI
│       ├── components/
│       │   ├── ConfigSection.tsx        # 配置区域（需更新）
│       │   └── ...
│       └── ...
```

---

## 🔄 重构内容

### 1. AI服务抽象化

**之前：**
- 硬编码 `GeminiService`
- 直接使用 `new GeminiService(apiKey)`

**现在：**
- 定义 `AIService` 接口
- 使用 `AIServiceFactory` 创建服务
- 支持多种模型切换

### 2. 配置结构优化

**之前：**
```typescript
{
  apiKey: string;
  // ...
}
```

**现在：**
```typescript
{
  aiModel: {
    type: 'gemini' | 'qwen' | 'kimi' | ...;
    apiKey: string;
    model?: string;
  };
  features: {
    autoGreet: boolean;
    autoRequestResume: boolean;
    autoAcceptResume: boolean;
  };
  // ...
}
```

### 3. 功能模块化

**新增：**
- `src/content/features/` - 功能模块目录
- `src/background/services/` - AI服务目录

**好处：**
- 代码组织更清晰
- 便于添加新功能
- 便于测试和维护

---

## 📝 文件说明

### AI服务相关

| 文件 | 说明 |
|------|------|
| `ai-service.interface.ts` | AI服务接口定义，所有模型必须实现 |
| `ai-config.ts` | 各AI模型的默认配置 |
| `base-ai-service.ts` | AI服务基类，提供通用功能 |
| `gemini-service.ts` | Gemini服务实现（已实现） |
| `qwen-service.ts` | 通义千问服务（待实现） |
| `kimi-service.ts` | Kimi服务（待实现） |

### 功能模块

| 文件 | 说明 |
|------|------|
| `chat-list-handler.ts` | 沟通列表处理（待实现） |

---

## 🚀 后续扩展步骤

### 添加新AI模型

1. 在 `src/background/services/` 创建服务文件
2. 实现 `AIService` 接口
3. 在 `AIServiceFactory` 中注册
4. 在 `ai-config.ts` 中添加配置
5. 更新UI添加模型选择

### 添加新功能

1. 在 `src/content/features/` 创建处理器
2. 添加DOM选择器
3. 集成到自动化控制器
4. 更新UI和配置

---

## ✅ 已完成

- ✅ 创建AI服务接口
- ✅ 重构Gemini服务
- ✅ 创建服务工厂
- ✅ 添加其他模型占位符
- ✅ 创建功能模块目录
- ✅ 创建沟通列表处理器框架

---

## 🔜 待完成

- ⏳ 实现通义千问服务
- ⏳ 实现Kimi服务
- ⏳ 更新UI支持模型选择
- ⏳ 实现沟通列表功能
- ⏳ 更新配置迁移逻辑（兼容旧版本）

---

**最后更新**：2025-01-29

