# Chrome扩展项目结构分析

## 一、项目目录结构

```
boss-greeting-assistant/
├── dist/                    ← ✅ 这是Chrome扩展的加载目录
│   ├── manifest.json       ← 构建后的manifest（使用编译后的JS路径）
│   ├── service-worker-loader.js  ← Service Worker入口
│   ├── assets/             ← 编译后的JS/CSS文件
│   │   ├── index.ts-BAwwYagw.js      (background脚本)
│   │   ├── index.ts-loader-CQKraxbS.js (content script)
│   │   ├── sidepanel-B6fJtba-.js     (sidepanel脚本)
│   │   └── ...
│   ├── icons/              ← 图标文件
│   └── src/
│       └── sidepanel/
│           └── index.html   ← Sidepanel HTML
│
├── src/                    ← 源代码目录（TypeScript/React）
│   ├── background/         ← Background脚本源码
│   ├── content/            ← Content脚本源码
│   └── sidepanel/          ← Sidepanel源码
│
├── public/                 ← 公共资源
│   ├── manifest.json       ← 源manifest（使用TypeScript路径）
│   └── icons/
│
└── package.json, vite.config.ts 等配置文件
```

## 二、Chrome扩展加载路径

### ✅ 正确：加载 `dist/` 目录

**路径**：`/Users/yingdao/Documents/Project/BOSS打招呼插件/boss-greeting-assistant/dist`

**原因**：
1. `dist/` 是构建输出目录，包含编译后的JavaScript文件
2. `dist/manifest.json` 使用编译后的JS路径（如 `service-worker-loader.js`）
3. Chrome扩展无法直接执行TypeScript源文件

### ❌ 错误：加载 `boss-greeting-assistant/` 根目录

**原因**：
1. 根目录包含TypeScript源文件（`.ts`），Chrome无法直接执行
2. `public/manifest.json` 使用TypeScript路径（如 `src/background/index.ts`）
3. 缺少编译后的JavaScript文件

## 三、dist目录结构验证

### ✅ 必需文件检查

1. **manifest.json** ✅
   - 位置：`dist/manifest.json`
   - Service Worker：`service-worker-loader.js` ✅
   - Sidepanel：`src/sidepanel/index.html` ✅
   - Content Script：`assets/index.ts-loader-CQKraxbS.js` ✅

2. **Service Worker** ✅
   - 入口：`dist/service-worker-loader.js`
   - 实际脚本：`dist/assets/index.ts-BAwwYagw.js`

3. **Sidepanel** ✅
   - HTML：`dist/src/sidepanel/index.html`
   - 脚本：`dist/assets/sidepanel-B6fJtba-.js`
   - 样式：`dist/assets/sidepanel-CZAXDa7i.css`

4. **Content Script** ✅
   - 脚本：`dist/assets/index.ts-loader-CQKraxbS.js`

5. **图标** ✅
   - `dist/icons/icon-16.svg`

## 四、潜在问题检查

### ⚠️ 问题1：Sidepanel HTML中的路径

**文件**：`dist/src/sidepanel/index.html`

**当前代码**：
```html
<script type="module" crossorigin src="/assets/sidepanel-B6fJtba-.js"></script>
```

**问题**：使用了绝对路径 `/assets/...`，在Chrome扩展中应该使用相对路径

**应该改为**：
```html
<script type="module" crossorigin src="../assets/sidepanel-B6fJtba-.js"></script>
```

**或者**：构建工具应该自动处理这个问题（@crxjs/vite-plugin应该会处理）

### ✅ 验证：构建工具处理

`@crxjs/vite-plugin` 应该会自动处理路径转换，所以这可能不是问题。

## 五、加载步骤

1. **打开Chrome扩展管理页面**
   - 地址：`chrome://extensions/`

2. **启用开发者模式**
   - 右上角打开"开发者模式"开关

3. **加载扩展**
   - 点击"加载已解压的扩展程序"
   - **选择目录**：`boss-greeting-assistant/dist`
   - **不要选择**：`boss-greeting-assistant/`（根目录）

4. **验证加载**
   - 检查是否有错误
   - 检查Service Worker是否正常运行
   - 检查Sidepanel是否可以打开

## 六、总结

### ✅ 正确的加载路径

**绝对路径**：
```
/Users/yingdao/Documents/Project/BOSS打招呼插件/boss-greeting-assistant/dist
```

**相对路径**（从项目根目录）：
```
boss-greeting-assistant/dist
```

### ✅ 目录层级正确性

- ✅ `dist/` 目录结构正确
- ✅ `manifest.json` 路径正确
- ✅ 所有必需文件都在正确位置
- ✅ 构建工具（@crxjs/vite-plugin）会自动处理路径转换

### 📝 注意事项

1. **每次代码修改后**：
   - 运行 `npm run build` 重新构建
   - 在Chrome扩展管理页面点击"重新加载"按钮

2. **如果遇到路径错误**：
   - 检查 `dist/manifest.json` 中的路径是否正确
   - 检查 `dist/src/sidepanel/index.html` 中的路径是否正确
   - 确认所有文件都在 `dist/` 目录中

